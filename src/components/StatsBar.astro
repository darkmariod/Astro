---
// src/components/StatsBar.astro
import StatsBarClient from './StatsBarClient.jsx';
import { getStats } from '../lib/sanity';

// Fetch data from Sanity if available
const stats = await getStats().catch(() => null);
---

<StatsBarClient client:visible stats={stats} />