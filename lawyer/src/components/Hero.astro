---
// src/components/Hero.astro
import HeroClient from './HeroClient.jsx';
import { getHeroSection } from '../lib/sanity';

// Fetch data from Sanity if available
const hero = await getHeroSection().catch(() => null);
---

<HeroClient client:visible hero={hero} />