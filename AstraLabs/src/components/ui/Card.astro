---
interface Props {
    title: string;
    description: string;
    image: string;
    date?: string;
    link?: string;
    category?: string;
    index?: number;
}

const { title, description, image, date, link = "#", category, index = 0 } = Astro.props;
---

<a 
    href={link} 
    class="group block bg-soil-raised rounded-xl overflow-hidden hover:ring-2 hover:ring-clay-600 transition-all duration-300 animate-card"
    data-animate-card
    data-index={index}
>
    <div class="relative h-44 sm:h-52 md:h-60 overflow-hidden">
        <img 
            src={image} 
            alt={title}
            class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
            loading="lazy"
        />
        {category && (
            <span class="absolute top-3 left-3 sm:top-4 sm:left-4 bg-clay-600 text-white text-xs font-bold uppercase px-3 py-1.5 rounded-full tracking-wider">
                {category}
            </span>
        )}
        <div class="absolute inset-0 bg-gradient-to-t from-soil-void/90 via-transparent to-transparent"></div>
    </div>
    <div class="p-5 sm:p-6 md:p-7">
        {date && (
            <p class="text-stone-400 text-sm mb-2">{date}</p>
        )}
        <h3 class="text-lg sm:text-xl md:text-2xl font-display font-bold text-white mb-3 group-hover:text-clay-400 transition-colors leading-tight">
            {title}
        </h3>
        <p class="text-stone-300 text-sm sm:text-base line-clamp-2 sm:line-clamp-3 leading-relaxed">
            {description}
        </p>
        <span class="inline-block mt-4 text-clay-400 text-sm font-semibold group-hover:translate-x-2 transition-transform">
            Ver más →
        </span>
    </div>
</a>

<style>
    .animate-card {
        opacity: 0;
        transform: translateY(40px);
        transition: opacity 0.5s ease-out, transform 0.5s ease-out, box-shadow 0.3s ease;
    }

    .animate-card.visible {
        opacity: 1;
        transform: translateY(0);
    }

    .animate-card:hover {
        box-shadow: 0 10px 40px rgba(184, 48, 38, 0.2);
    }
</style>
