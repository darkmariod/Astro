---
// src/components/LogoCarousel.astro
import LogoCarouselClient from './LogoCarouselClient.jsx';
import { getClientes } from '../lib/sanity';

// Fetch data from Sanity if available
const clientes = await getClientes().catch(() => null);
---

<LogoCarouselClient client:load clientes={clientes} />